name: CI

on:
  push:
  pull_request:

jobs:
  php-minimum:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer
      - run: composer install --working-dir=sdk --no-interaction --no-progress
      - run: composer validate --working-dir=sdk --strict
      - run: composer analyse --working-dir=sdk
      - run: composer test --working-dir=sdk
      - run: find examples -path '*/vendor' -prune -o -name '*.php' -exec php -l {} \;

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          tools: composer
      - run: test -z "$(gofmt -l cmd internal tools)"
      - run: go vet ./...
      - run: composer install --working-dir=sdk --no-interaction --no-progress
      - run: composer validate --working-dir=sdk --strict
      - run: composer analyse --working-dir=sdk
      - run: composer test --working-dir=sdk
      - run: for example in examples/*/composer.json; do composer install --working-dir="$(dirname "$example")" --no-interaction --no-progress; done
      - run: go run ./tools/codegen
      - run: git diff --exit-code -- sdk/src/Internal/api_generated.php sdk/src/Api sdk/src/Event/Events.php sdk/src/Event/Handlers.php sdk/src/Constant cmd/component/native_generated.go cmd/component/native_generated.h cmd/component/events_generated.go cmd/component/events_generated.h
      - run: go test -race -short -timeout=3m ./...
      - run: GOARCH=amd64 CGO_ENABLED=1 go build -mod=vendor -buildmode=c-shared -o build/ompphp.so ./cmd/component
      - run: ./tests/e2e-openmp.sh --skip-build

  windows-e2e:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          extensions: zip
          tools: composer
      - uses: msys2/setup-msys2@v2
        id: msys2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc
      - shell: pwsh
        run: |
          $env:CGO_ENABLED = '1'
          $env:GOARCH = 'amd64'
          $env:CC = Join-Path '${{ steps.msys2.outputs.msys2-location }}' 'mingw64\bin\gcc.exe'
          if (!(Test-Path $env:CC)) { throw "MinGW compiler not found at $env:CC" }
          go test ./internal/runtime
          if ($LASTEXITCODE -ne 0) { throw "Windows runtime tests failed with exit code $LASTEXITCODE" }
          go build -mod=vendor -buildmode=c-shared -o build/ompphp.dll ./cmd/component
      - shell: pwsh
        run: ./tests/e2e-openmp.ps1
